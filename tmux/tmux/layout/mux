session_root "~"
if initialize_session mux/`whoami`/`hostname`; then
    new_window weechat
    run_cmd weechat
    new_window ncmpcpp
    run_cmd ncmpcpp
    new_window neomutt
    run_cmd neomutt
    new_window newsboat
    run_cmd newsboat
fi
finalize_and_go_to_session
