XDG_CONFIG_HOME           DEFAULT=@{HOME}/.config
XDG_CACHE_HOME            DEFAULT=@{HOME}/.cache
XDG_DATA_HOME             DEFAULT=@{HOME}/.local/share
GNUPGHOME                 DEFAULT=@{HOME}/.gnupg
EDITOR                    DEFAULT=vim
VISUAL                    DEFAULT=vim
PAGER                     DEFAULT=less
LESS                      DEFAULT=-RFX
LESSOPEN                  DEFAULT=|lesspipe %s
BROWSER                   DEFAULT=firefox
SVDIR                     DEFAULT=@{HOME}/.usv
MAIL                      DEFAULT=@{HOME}/.mail/mailbox/INBOX
SSH_AGENT_PID             DEFAULT=
SSH_AUTH_SOCK             DEFAULT=${GNUPGHOME}/S.gpg-agent.ssh
DBUS_SESSION_BUS_ADDRESS  DEFAULT=unix:path=@{HOME}/.dbus-session
TESTVAR                   DEFAULT=testvar
